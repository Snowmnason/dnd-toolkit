/**
 * Developer-approved external URL origins.
 *
 * These origins are always trusted by the navigation system — no user consent modal is shown.
 * Add new origins here after team verification that they are safe and stable.
 *
 * Format: `https://hostname` (must include scheme and hostname; no path or query string).
 *
 * Examples:
 * - 'https://dnd-wiki.com'
 * - 'https://dnd-beyond.com'
 */
export const DEV_APPROVED_ORIGINS: string[] = [
  // Add verified external links here, e.g.:
  // 'https://dnd-wiki.com',
  // 'https://dnd-beyond.com',
];
