{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "description": {
      "type": "string"
    },
    "flags": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "enabled": {
            "type": "boolean"
          },
          "description": {
            "type": "string"
          },
          "kind": {
            "type": "string",
            "enum": ["free", "premium", "beta"],
            "description": "Optional classification of the feature"
          }
        },
        "required": ["enabled"]
      }
    }
  },
  "required": ["flags"]
}
