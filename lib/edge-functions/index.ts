/**
 * Edge Functions Module
 *
 * Central registry and helpers for Supabase Edge Function endpoints.
 * All URLs are defined in constants.ts and can be used to build full endpoints.
 */

export {
    EDGE_FUNCTIONS,
    getEdgeFunctionUrl,
    getHealthEndpointUrl
} from "./constants";

