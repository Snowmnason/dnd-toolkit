/**
 * Version constant - synced from package.json
 * Update package.json version when releasing a new version
 */
import packageJson from "../../package.json";

export const APP_VERSION = packageJson.version;
